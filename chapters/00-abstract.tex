% chapters/00-abstract.tex -- Реферат (українською та іноземною мовою).
%
% Вимоги: ДСТУ 3008:2015, п. 4.3; для магістерської дисертації --
% додатково розділ "Реферат" Рекомендацій КПІ (до 500 слів, певна
% послідовність пунктів). Замініть приклад нижче своїм текстом,
% зберігаючи структуру.
\kpithesisUnnumberedChapter{Реферат}

Пояснювальна записка до \workType: \pageref{LastPage}~с.,
\arabic{figure}~рис., \arabic{table}~табл., 2~додатки, 00~джерел.
% ^^^ \pageref{LastPage} працює, лише якщо в кінці документа є мітка
% \label{LastPage} (вона вже додана в main.tex). Кількість джерел
% після підключення бібліографії (Фаза 5) можна порахувати вручну.

\textbf{Об'єкт дослідження} -- \dots

\textbf{Мета роботи} -- \dots

\textbf{Методи дослідження} -- \dots

\textbf{Наукова новизна} (для магістерської дисертації) -- \dots

\textbf{Практичне значення} -- \dots

% ДСТУ 3008:2015, п. 4.3.8: ключові слова друкують ПРОПИСНИМИ
% (великими) літерами -- на відміну від решти тексту реферату.
% Жодних \bigskip тут не треба: звичайний абзацний відступ і
% звичайний проміжок між абзацами вже відокремлюють цей рядок від
% сусідніх настільки, наскільки потрібно (див. docs/ARCHITECTURE.md,
% розділ про \baselineskip, чому зайвий \bigskip тут лише зіпсував би
% рівномірність).
\textbf{Ключові слова:} \MakeUppercase{ключове слово 1, ключове слово 2,
ключове слово 3}.

\begin{english}
  % "ABSTRACT" оформлено як заголовок (великі літери, жирний,
  % посередині), а не просто жирне слово в тексті -- щоб мати ту саму
  % візуальну вагу, що й "РЕФЕРАТ" вище. Інакше він губиться поруч із
  % повноцінним заголовком розділу.
  %
  % Свідомо НЕ \begin{center}...\end{center}: це оточення має власний
  % умовчаний відступ (\topsep), не пов'язаний із \baselineskip, тому
  % дає "некруглу" відстань (≈1,75 інтервала). \centering всередині
  % групи { } так само центрує текст, але без цього зайвого відступу
  % -- тоді відстань керується тільки нашим \vspace{1\baselineskip}
  % (= рівно 2 інтервали з тих самих причин, що й у заголовків розділів
  % -- див. docs/ARCHITECTURE.md).
  \vspace{1\baselineskip}
  {\centering\bfseries ABSTRACT\par}
  \vspace{1\baselineskip}

  Explanatory note to the \dots: \pageref{LastPage}~p., \arabic{figure}~fig.,
  \arabic{table}~tables, 2~appendices, 00~references.

  \textbf{Object of research} -- \dots

  \textbf{Purpose} -- \dots

  \textbf{Methods} -- \dots

  \textbf{Scientific novelty} -- \dots

  \textbf{Practical value} -- \dots

  \textbf{Keywords:} \MakeUppercase{keyword one, keyword two, keyword three}.
\end{english}
